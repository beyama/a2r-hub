extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href="#", onclick="sendData();")
    "Foo"

  script
    var ws = new WebSocket('ws://192.168.1.100:8080');
    ws.onmessage = function (event) {
      console.log(event);
    };
